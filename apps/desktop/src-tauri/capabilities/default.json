{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main app window.",
  "windows": ["main", "pet"],
  "permissions": [
    "core:default",
    "notification:default",
    "autostart:default",
    "dialog:default",
    "opener:default"
  ]
}
